#!/bin/bash

# url="https://example.com/path/to/package/ubuntu20.04-amd64.tar.gz"

# # Check if the URL exists and ends with the expected file extension
# if curl --output /dev/null --silent --head --fail "$url" && \
#    [[ "$url" =~ (ubuntu20.04-amd64.tar.gz|ubuntu20.04-amd64.zip)$ ]]; then
#     exit 0  # URL exists and meets conditions
# else
#     exit 1  # URL does not exist or does not meet conditions
# fi
